# Loaded automatically by `npm start` (Create React App applies
# .env.development in development mode). No secrets — this only points
# the frontend at the local backend API.
REACT_APP_API_URL=http://localhost:5000
